system:
  mode: "live"
  log_level: "INFO"
  timezone: "Asia/Shanghai"

server:
  host: "0.0.0.0"
  port: 7000
  dashboard_refresh_rate: 3

data:
  provider: "sina" 
  tushare_token: "YOUR_TUSHARE_TOKEN"
  cache:
    enabled: true